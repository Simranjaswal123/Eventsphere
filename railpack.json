{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "extensions": {
      "command": "install-php-extensions mongodb"
    },
    "install:composer": {
      "command": "composer install --optimize-autoloader --no-scripts --no-interaction --ignore-platform-req=ext-mongodb"
    }
  }
}
